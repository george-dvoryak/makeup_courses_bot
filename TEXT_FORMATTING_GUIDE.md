# Форматирование текста в Google Sheets для Telegram

## Как использовать форматирование

В Google Sheets можно использовать **HTML-теги**, которые будут автоматически преобразованы в форматирование Telegram.

## Поддерживаемые теги Telegram

| HTML-тег | Telegram | Описание | Пример |
|----------|----------|----------|---------|
| `<b>` или `<strong>` | **жирный** | Жирный текст | `<b>важный текст</b>` |
| `<i>` или `<em>` | *курсив* | Курсив | `<i>курсивный текст</i>` |
| `<u>` | <u>подчеркнутый</u> | Подчеркивание | `<u>подчеркнутый</u>` |
| `<s>` или `<strike>` | ~~зачеркнутый~~ | Зачеркивание | `<s>зачеркнутый</s>` |
| `<del>` | ~~удаленный~~ | Удаленный текст | `<del>удаленный</del>` |
| `<code>` | `код` | Моноширинный шрифт | `<code>print("hello")</code>` |
| `<pre>` | ```блок кода``` | Блок кода | `<pre>function test() {}</pre>` |
| `<a href="url">` | [ссылка](url) | Ссылка | `<a href="https://google.com">Google</a>` |

## Абзацы и переносы строк

### Способ 1: ALT+Enter в Google Sheets (рекомендуется)
В ячейке Google Sheets нажмите **ALT + Enter** для создания новой строки.

**Пример в таблице:**
```
Привет!

Это новый абзац.

И еще один абзац.
```

### Способ 2: Использовать `\n` в тексте
Можно написать `\n` в тексте, бот автоматически преобразует его в перенос строки.

**Пример в таблице:**
```
Привет!\n\nЭто новый абзац.\n\nИ еще один абзац.
```

**Результат в Telegram:**
```
Привет!

Это новый абзац.

И еще один абзац.
```

### Важно:
- Для абзацев используйте **пустую строку** между ними
- В Google Sheets пустая строка = двойной перенос в Telegram
- Символы `\n` будут автоматически преобразованы в реальные переносы

## Примеры использования

### 1. Приветственное сообщение
```
<b>Добро пожаловать!</b>

Я помогу вам выбрать и оплатить курс.
Для начала нажмите <b>"Каталог"</b>
```

**Результат:**
**Добро пожаловать!**

Я помогу вам выбрать и оплатить курс.
Для начала нажмите **"Каталог"**

### 2. Текст поддержки
```
<b>Поддержка</b>

Если у вас возникли вопросы, пишите нам:
• <a href="https://t.me/support">Telegram</a>
• <a href="mailto:info@example.com">Email</a>

<i>Мы ответим в течение 24 часов</i>
```

**Результат:**
**Поддержка**

Если у вас возникли вопросы, пишите нам:
• [Telegram](https://t.me/support)
• [Email](mailto:info@example.com)

*Мы ответим в течение 24 часов*

### 3. Описание курса
```
<b>Курс "Мастер-класс по макияжу"</b>

<i>Автор: Анна Иванова</i>

Что вы изучите:
• Основы макияжа
• <b>Профессиональные техники</b>
• <code>Практические упражнения</code>

<u>Стоимость: 2990₽</u>
```

## Важные правила

### ✅ Правильно
```html
<b>жирный</b> и <i>курсив</i>
Текст с <a href="https://site.com">ссылкой</a>
```

### ❌ Неправильно
```html
<!-- Не используйте неподдерживаемые теги -->
<h1>Заголовок</h1>  <!-- Будет экранирован -->
<div>Текст</div>    <!-- Будет экранирован -->
<img src="pic.jpg"> <!-- Будет экранирован -->

<!-- Не используйте незакрытые теги -->
<b>текст            <!-- Должен быть <b>текст</b> -->
```

### 📝 Советы
1. **Всегда закрывайте теги**: `<b>текст</b>`, а не `<b>текст`
2. **Используйте только поддерживаемые теги** (список выше)
3. **Для ссылок используйте**: `<a href="https://site.com">текст ссылки</a>`
4. **Для абзацев**: двойной перенос строки `\n\n`
5. **Проверяйте результат** в Telegram после сохранения

## Тестирование

После добавления текста в Google Sheets:

1. Сохраните таблицу
2. Перезагрузите бота
3. Отправьте команду `/start` или нажмите нужную кнопку
4. Проверьте, что форматирование отображается правильно

## Отладка

Если форматирование не работает:
1. Проверьте, что теги правильно закрыты
2. Убедитесь, что используете только поддерживаемые теги
3. Проверьте логи сервера на ошибки
4. Запустите `python3 debug_texts_simple.py` для проверки загрузки текстов
